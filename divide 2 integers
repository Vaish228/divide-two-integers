int divide(int dividend, int divisor) {
    double a=(double)dividend;
    double b=(double)divisor;

    if(b==0)
       return 0;
    else if(a<-1 && b==-1)
       return abs(a-b);   
    else
    {   
    double c =(dividend/divisor);
    return trunc(c);
    }
}
